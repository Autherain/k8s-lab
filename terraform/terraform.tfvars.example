# =============================================================================
# EXEMPLE DE CONFIGURATION - terraform.tfvars.example
# =============================================================================
#
# COMMENT UTILISER CE FICHIER :
# 1. Copie ce fichier vers terraform.tfvars :
#    cp terraform.tfvars.example terraform.tfvars
#
# 2. Modifie les valeurs selon tes besoins
#
# 3. terraform.tfvars est automatiquement lu par Terraform
#
# ⚠️ IMPORTANT : terraform.tfvars contient potentiellement des secrets,
#    il est exclu du git via .gitignore
#
# =============================================================================

# -----------------------------------------------------------------------------
# NOM DU PROJET
# -----------------------------------------------------------------------------
# Préfixe pour toutes les ressources créées
# Utile si tu as plusieurs labs

project_name = "k8s-lab"

# Image Scaleway (nom exact trouvé via marketplace)
image_name = "ubuntu_jammy"

# -----------------------------------------------------------------------------
# TAILLE DES VMS
# -----------------------------------------------------------------------------
# Control-plane : DEV1-M minimum recommandé (3 vCPU, 4GB RAM)
# Worker : DEV1-M minimum recommandé

control_plane_flavor = "DEV1-M"
worker_flavor        = "DEV1-M"

# -----------------------------------------------------------------------------
# CLÉ SSH
# -----------------------------------------------------------------------------
# La clé SSH est maintenant générée automatiquement par Terraform
# Elle est stockée dans le tfstate (dans S3), récupérable même si on perd le repo
# Plus besoin de configurer de chemin de clé SSH manuellement !

# -----------------------------------------------------------------------------
# SÉCURITÉ
# -----------------------------------------------------------------------------
# IP autorisée pour SSH et API Kubernetes
# Pour plus de sécurité, mets ton IP : "1.2.3.4/32"
# Pour trouver ton IP : curl ifconfig.me

allowed_ssh_cidr = "0.0.0.0/0"

# -----------------------------------------------------------------------------
# SCALEWAY (RÉGION / ZONE)
# -----------------------------------------------------------------------------
# La région doit correspondre à celle du bucket Object Storage

scaleway_region = "fr-par"
scaleway_zone   = "fr-par-1"

