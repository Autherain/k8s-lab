# =============================================================================
# GITIGNORE TERRAFORM
# =============================================================================
# Ces fichiers ne doivent PAS être commités car ils contiennent :
# - Des secrets (credentials)
# - L'état du déploiement (terraform.tfstate)
# - Des fichiers générés localement
# =============================================================================

# -----------------------------------------------------------------------------
# ÉTAT TERRAFORM
# -----------------------------------------------------------------------------
# ⚠️ SUPER IMPORTANT :
# terraform.tfstate contient TOUT l'état de ton infrastructure.
# Si tu le perds, Terraform ne sait plus ce qu'il a créé.
# Si tu le commites, tes secrets sont exposés.
#
# Pour un vrai projet, on utilise un "remote backend" (S3, etc.)
# Pour un lab, on le garde local mais on le sauvegarde manuellement.

*.tfstate
*.tfstate.*
.terraform.tfstate.lock.info

# -----------------------------------------------------------------------------
# RÉPERTOIRE DES PLUGINS
# -----------------------------------------------------------------------------
# Téléchargé par "terraform init", pas besoin de le versionner

.terraform/

# -----------------------------------------------------------------------------
# FICHIERS DE VARIABLES AVEC SECRETS
# -----------------------------------------------------------------------------
# terraform.tfvars peut contenir des credentials
# On garde .tfvars.example comme modèle

terraform.tfvars
*.auto.tfvars

# -----------------------------------------------------------------------------
# CONFIGURATION BACKEND AVEC CREDENTIALS
# -----------------------------------------------------------------------------
# backend.json ou backend.hcl contiennent les credentials OVH Object Storage
# On garde backend.json.example et backend.hcl.example comme modèles

backend.json
backend.hcl
backend-config.tfvars

# -----------------------------------------------------------------------------
# FICHIERS DE CRASH ET DEBUG
# -----------------------------------------------------------------------------

crash.log
crash.*.log

# -----------------------------------------------------------------------------
# FICHIERS GÉNÉRÉS
# -----------------------------------------------------------------------------

*.tfplan
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# -----------------------------------------------------------------------------
# FICHIERS D'IDE
# -----------------------------------------------------------------------------

.idea/
*.swp
*.swo
*~

# -----------------------------------------------------------------------------
# FICHIERS MACOS
# -----------------------------------------------------------------------------

.DS_Store

